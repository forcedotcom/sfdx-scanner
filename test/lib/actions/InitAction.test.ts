//import {InitAction} from '../../../src/lib/actions/InitAction';

describe('InitAction tests', () => {
//	it('Invoke #execute() to enforce coverage', () => {
//		new InitAction().execute({});
//	});
});
