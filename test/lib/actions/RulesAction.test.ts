import {RulesAction} from '../../../src/lib/actions/RulesAction';

describe('RulesAction tests', () => {
	it('Invoke #execute() to enforce coverage', () => {
		new RulesAction().execute({});
	});
});
